pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // 国内镜像（可选，删掉也不影响）
        maven { url 'https://maven.aliyun.com/repository/public' }
    }
}

rootProject.name = "SignLanguageApp"
include ':app'
